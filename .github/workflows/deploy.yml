name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Atualizar servidor
        run: |
          cd /data/teste-deploy

          git fetch origin

          git reset --hard origin/main

          docker compose up -d --build