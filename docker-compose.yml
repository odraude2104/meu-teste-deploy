services:
  nginx:
    image: nginx:latest
    container_name: nginx-teste
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
