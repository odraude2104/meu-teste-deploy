services:
  nginx:
    image: nginx:latest
    container_name: nginx-teste
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      # Monta a pasta atual (onde está o index.html) na pasta do nginx
      - ./:/usr/share/nginx/html:ro
