services:
  nginx:
    image: nginx:latest
    container_name: meu-teste-deploy
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      # Mapeia a pasta atual do host para a pasta de arquivos do Nginx
      - ./:/usr/share/nginx/html:ro
